# Origins the gateway proxies to based on subdomain (see src/proxy.ts).
# In development these point at each app's local dev server; in production
# they should point at each app's own deployed origin.
CONSENT_ORIGIN=http://localhost:3001
EMAIL_ORIGIN=http://localhost:3002
ADMIN_ORIGIN=http://localhost:3003
TLP_ORIGIN=http://localhost:3004
API_ORIGIN=http://localhost:3005
